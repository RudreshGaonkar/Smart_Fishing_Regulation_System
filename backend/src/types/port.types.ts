// ============================================================
// Port Types — backend/src/types/port.types.ts
// ============================================================

export interface Port {
  port_id: number;
  name: string;
  latitude: number;
  longitude: number;
  created_at: string;
}
